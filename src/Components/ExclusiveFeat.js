export default function ExclusiveFeat({ propData }) {
    
    // const data = propData.find((item) => item.type === "features");

    return (
        <>
            <section>
                <div>{data?.name}</div>

               
            </section>
        </>
    )
}