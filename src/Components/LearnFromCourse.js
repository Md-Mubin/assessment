export default function LearnFromCourse({ propData }) {
    
    // const data = propData.find((item) => item.type === "features");

    return (
        <>
            <section>

               
            </section>
        </>
    )
}