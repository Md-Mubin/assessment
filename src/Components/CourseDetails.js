export default function CourseDetails({ propData }) {
    
    // const data = propData.find((item) => item.type === "features");

    return (
        <>
            <section>

               
            </section>
        </>
    )
}